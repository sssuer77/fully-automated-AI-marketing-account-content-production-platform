# AI全自动营销号制片台 · 可执行方案稿（文字版）

> 范围：短视频营销号全自动运营，覆盖从选题、写稿、配音、模板化合成、渲染到发布的完整链路。
> 原则：完全AI自动运营，人工只做三件事——定目标、充钱、看钱。
> 部署：全部子系统本地部署运行，通过网页端实时操作。
> 本稿为可执行方案说明，不涉及具体工程代码。

---

## 第一部分 总体架构

### 1.1 系统总览

整个制片台由五大子系统组成，按流水线顺序排列：

```
                 ┌──────────────────────────────┐
                 │  ⓞ 网页端实时操作台 (Web UI)   │
                 │  所有子系统的唯一入口，浏览器访问  │
                 └──────────────┬───────────────┘
                                │
① 智能体脚手架（选题→写稿→审稿）
        ↓
② CosyVoice3 配音（熊大/熊二音色复刻，按句保存）
        ↓
③ 三层模板渲染引擎（视频模板→场景模板→组件，自动填充）
        ↓
④ 任务队列（三池并行调度，断点续传）
        ↓
⑤ 成片与发布（衔接制片台既有设计）
```

数据流核心：**一篇新稿从进入系统到出成片，全程零人工剪辑**。
操作核心：**一切动作都在网页端实时完成，本机一键启动全部服务。**

### 1.2 设计原则

1. **模板驱动**：视频不是"剪"出来的，是"填"出来的。同一套模板换稿件即可无限出片。
2. **资源解耦**：写稿（吃CPU/LLM额度）、配音（吃GPU/显存）、渲染（吃CPU/内存）三者互不阻塞。
3. **断点续传**：任何环节中断都能从断点继续，不重跑已完成工作。
4. **可降级**：任一子系统不可用时（无GPU/无模型/无素材），其余环节照常运行。
5. **本地优先**：LLM、TTS、渲染、数据库全部跑在本机，数据不出机器。
6. **网页实时**：任务状态、进度、稿件、成片全部通过网页端实时查看与操作，无需命令行。

---

## 第二部分 智能体脚手架：选题→写稿

### 2.1 系统输入（三样东西）

| 输入 | 来源 | 说明 |
|---|---|---|
| 频道定位 | config/persona.yaml | 人设/口吻/受众/口癖/禁区，一次设定永久生效 |
| 历史反馈 | data/feedback/*.md | 评论、差评、用户期望、走势，自动解析情感与诉求 |
| 热点信息 | data/hot/*.md | 手动粘贴或爬虫导入，每行"标题\|热度\|平台" |

### 2.2 处理管线（八步）

```
[1] 方向分析 → [2] 批量选题 → [3] 内容导演 → [4] 写稿
        → [5] 审稿 → [6] 改稿 ⇄ 审稿(循环) → [7] 人工确认闸 → [8] 配音
```

**① 方向分析（Planner）**
- 综合频道定位 + 历史反馈 + 当前热点，输出5-8个内容方向。
- 规则：方向紧扣定位不跑偏；优先历史反馈中"用户想要"的方向；回避"被吐槽"的方向；紧贴热点选有流量潜力的。

**② 批量选题（Ideator）**
- 对每个方向批量产出4个选题，一次可达20+选题。
- 选题要求：标题有网感、有钩子（冲突/悬念/反差/数字）、角度差异化、3分钟以内可执行。

**③ 内容导演（Director）**
- 每个选题展开成脚本大纲：黄金3秒钩子、正文3-5段（每段含要点+画面建议+情绪）、结尾CTA、时长预估。

**④ 写稿（Writer）**
- 按大纲+人设产出600-800字完整口播稿。
- 严格要求：口吻/口癖必须带上；口语化、句子短、适合朗读；每300字一个段落（为后续按句配音做切分）；绝不碰禁区。

**⑤ 审稿（Reviewer）双通道评分**
- 规则通道：稿件长度、禁区词命中、开场/结尾完整性、段落重复度。
- LLM通道：六维度打分（开场钩子/定位契合/口语化/情绪节奏/结尾引导/禁区）。
- 最终分 = 0.3×规则分 + 0.7×LLM分，分级：A(≥8直接过) / B(5-7.9需改) / C(<5废弃)。

**⑥ 改稿（Editor）**
- 按审稿意见修改稿件，只改被指出的问题，保留原文优点；每次修改记录版本号，最多改2轮。

**⑦ 人工确认闸（确认步骤）**
- 审稿通过后暂停，把稿件+评分+修改次数呈现给人工。
- 三个选项：确认（继续配音）/ 退回改写（带意见再改一轮）/ 放弃该稿。
- 这是整个流水线唯一保留的人工介入点，保证内容不失控。

**⑧ 配音（接下一部分）**

### 2.3 产出物

```
data/output/topics/  选题卡（5备选标题+大纲+评分+理由）
data/output/voice/   按句配音wav + 全稿拼接wav
data/output/videos/  最终成片mp4
```

---

## 第三部分 CosyVoice3 配音：熊大熊二音色复刻

### 3.1 音色资产

| 音色ID | 角色 | 需准备原声 |
|---|---|---|
| bigbear | 熊大（熊出没） | 2-3段干净wav，每段10-30秒，无背景音乐 |
| littlebear | 熊二（熊出没） | 同上 |

原声放置：`data/voice_src/bigbear/`、`data/voice_src/littlebear/`。

### 3.2 复刻原理

使用本地部署的 CosyVoice3（FunAudioLLM开源项目），**零样本复刻**：
`inference_zero_shot(目标文本, 参考文本, 熊大原声wav)` → 用原声音色说任意新台词。
- 全程本地推理，Windows+GPU环境，数据不出机器。
- 复刻完成后，系统可永久用"熊大/熊二"音色朗读任意稿件。

### 3.3 按句保存（关键设计）

**配音不是一稿一文件，而是逐句保存、句句可续。**

```
稿件段落 → 拆句 → 逐句合成 wav → 按句落盘到 data/output/voice/句子序号.wav
```

- 每一句配音保存时同步记录状态（未开始/合成中/完成/失败）。
- **中途停机后再次运行，自动跳过已完成句子，只续合成未完成的**，不会整稿重跑。
- 按句保存的好处：
  1. 中断续传（停电/死机/手动停都不丢已完成的句子）
  2. 单句失败只重合成该句，不浪费整稿
  3. 渲染引擎可逐句消费，边配音边渲染

### 3.4 降级策略

- CosyVoice3 未安装或模型缺失时，配音环节自动跳过，其余环节照常跑完（前端流程不受阻）。
- 输出台账记录"该稿未配音"，供后续补齐。

---

## 第四部分 三层模板渲染引擎

### 4.1 三层结构

```
┌─ 视频模板（一套模具）─────────────────────────────┐
│  ├─ 片头场景（标题 + 熊大贴图 + 开场BGM）         │
│  ├─ 主画面场景（字幕 + 背景视频 + 配音 + 贴图）×N  │
│  └─ 片尾场景（关注引导 + 收尾BGM）                │
└──────────────────────────────────────────────────┘
```

**第一层：视频模板**（相当于模具）
- 定义整支视频的骨架：片头/主画面/片尾的排列顺序与时长节奏。
- 内含若干场景模板的实例（引用关系）。
- 参数：分辨率（默认1080×1920竖屏）、帧率、布局序列。

**第二层：场景模板**（可复用的积木块）
- 每个场景是一个独立积木，可被多个视频模板引用。
- 场景内可放置任意多个组件。
- 两种时长模式：固定时长（如片头3秒）/ 跟随内容（如主画面时长=配音长度）。

**第三层：组件**（场景里的零件）
| 组件类型 | 作用 | 示例 |
|---|---|---|
| 标题 | 大字标题层 | 片头"熊大熊大有话说" |
| 字幕 | 逐句字幕层 | 主画面每句配音配字幕 |
| 贴图 | 熊大/熊二形象贴图 | 常驻画面右下角 |
| 背景视频 | 底层画面 | 我的世界跑酷素材 |
| 音乐 | BGM | 片头紧张/主画面欢乐/片尾轻快 |
| 配音 | 对白音频 | 熊大/熊二朗读的句子 |

### 4.2 背景视频：我的世界跑酷

- 使用用户自有的我的世界跑酷素材包，存放于 `data/assets/mc_parkour/`。
- 模板支持通配引用（如 `parkour_*.mp4`），渲染时随机抽取，**同模板不同片次画面不同**，防止内容重复被判搬运。
- 无素材时降级：自动黑屏+纯字幕/标题出片，保证链路不断。

### 4.3 自动填充机制（核心价值）

```
新稿导入
  ↓
标题 → 自动填入【片头场景】的标题组件
配音 → 自动填入【主画面场景】的配音组件（按句分配）
字幕 → 自动填入【主画面场景】的字幕组件（与配音句对齐）
背景 → 自动从素材库抽取我的世界跑酷画面
BGM  → 模板自带，全局不变
  ↓
直接渲染出片
```

- **同一模板 + 换一篇稿子 = 直接再生成**，不需要重新剪辑。
- 段落与主画面场景的对应：段落数 > 场景数时自动复制最后一个主画面场景继续填，无限扩展。
- 字幕自动换行（每行限字）、描边、居中，位置由模板样式控制。

### 4.4 渲染引擎

- 使用 ffmpeg 合成：背景视频缩放裁剪铺满 → 叠加贴图/字幕/标题透明层 → 混入配音与BGM → 输出H.264 mp4。
- 每个场景独立合成 → 全部场景按顺序拼接 → 输出最终成片。
- 渲染产物：`data/output/videos/{时间戳}_final.mp4`。

---

## 第五部分 任务队列：并行调度

### 5.1 为什么需要并行

三个环节占用资源完全不同：

| 环节 | 主要资源 | 特点 |
|---|---|---|
| 写稿 | LLM Token 额度 | 快（秒级），只吃API |
| 配音 | GPU显存 | 中速（句级），独占GPU |
| 渲染 | CPU+内存 | 慢（分钟级），吃满CPU |

**如果串行执行**：一篇视频的写稿→配音→渲染跑完，其他稿件全部排队空转，GPU、CPU大量闲置。
**如果并行执行**：写稿快，可以同时推进多篇；配音占GPU，可以边跑渲染。

### 5.2 三池并行模型

```
           ┌─ 写稿Worker池（2个）── 同时写2篇稿
任务队列 ──┤─ 配音Worker池（3个）── 同时配3篇（或3句）
           └─ 渲染Worker池（2个）── 同时渲染2支
```

**一个视频在写稿时，另一个可以配音，还有一个在渲染导出**，三条流水线同时转。

- 写稿快的多，可以让写稿池不停地产稿，喂给配音池。
- 配音池吃GPU，数量按显存设（如3并发）。
- 渲染池吃CPU，数量按核心数设（如2并发）。
- 每个任务完成后自动触发下游任务入队（写稿完→配音任务入队→配音完→渲染任务入队）。

### 5.3 任务状态机

```
新稿 → 待写稿 → 写稿中 → 待配音 → 配音中 → 待渲染 → 渲染中 → 已完成
                      ↓失败        ↓失败         ↓失败
                     重试(3次)     重试(3次)     重试(3次)
                     超限→人工池  超限→跳过    超限→降清晰度出片
```

### 5.4 断点续传（基于按句配音）

- 全部任务状态与进度**持久化到本地数据库（SQLite）**，进程重启不丢失。
- 配音进度按句记录：启动时扫描，已完成句子直接跳过。
- 渲染进度按场景记录：已合成完的场景不重渲。
- 任何时刻 Ctrl+C / 断电 / 重启，下次启动自动从断点继续。

### 5.5 降级与容错

- 任务失败自动重试3次（指数退避）。
- 配音失败可跳过该句继续；渲染失败降为720P出片保底。
- 单篇反复失败进入"人工池"待处理，不影响其他任务。

---

## 第七部分 本地部署与网页端实时操作台

### 7.1 全部本地部署

所有子系统部署在同一台本地机器上，数据不出机器：

| 子系统 | 部署位置 | 说明 |
|---|---|---|
| LLM 文案Agent | 本地 | OpenAI兼容接口，可选本地模型（Ollama/DeepSeek本地版）或云端API |
| CosyVoice3 配音 | 本地 GPU | 模型权重在本机，音色复刻不出机器 |
| 三层模板渲染 | 本地 CPU | ffmpeg 本机渲染，素材包在本机 |
| 任务队列/数据库 | 本地 | SQLite 存于本机磁盘 |
| 网页操作台 | 本地 | 本机起一个 Web 服务，浏览器访问 localhost |

**一键启动**：双击启动脚本 → 自动拉起所有服务 → 浏览器自动打开操作台。

### 7.2 网页端实时操作台（Web UI）

**访问方式**：本机浏览器访问 `http://localhost:xxxx`（也可配局域网IP供手机/其他设备访问）。

**实时性**：WebSocket 长连接推送，任务进度实时刷新，无需手动刷新页面。

#### 网页端功能面板一览

| 面板 | 实时内容 | 可执行操作 |
|---|---|---|
| 总览台 | 三池并行状态、队列长度、今日产量、资源占用 | 启动/暂停/一键全自动 |
| 选题面板 | 方向列表、批量选题卡片、评分理由 | 导入热点、触发分析、人工加选题 |
| 稿件面板 | 稿件全文、审稿评分、修改版本 | 点击【人工确认闸】确认/退回/放弃 |
| 配音面板 | 每句配音进度条（已完成/进行中/失败） | 换音色、重配某句、试听单句 |
| 渲染面板 | 场景合成进度、ffmpeg日志、成片列表 | 换模板、触发渲染、成片在线播放 |
| 模板面板 | 视频模板/场景/组件可视化编辑 | 拖拽加组件、改样式、上传素材 |
| 素材库 | 我的世界跑酷素材、熊大熊二原声、BGM | 上传、预览、标记 |
| 实时日志 | 滚动输出所有子系统日志 | 过滤级别 |

### 7.3 网页端关键操作（对应流水线各环节）

**① 选题环节**：网页粘贴热点 → 点【AI分析方向】→ 方向卡片出现 → 点【批量选题】→ 选题瀑布流 → 勾选要拍的选题 → 入队。

**② 写稿环节**：选题自动进写稿池 → 稿件面板实时看到"写稿中→已完成"，评分自动显示。

**③ 人工确认闸**：稿件完成时网页弹出确认卡片（全自动模式下A级自动放行，B级等人工点一下）。

**④ 配音环节**：确认后自动进配音池 → 配音面板逐句刷新进度条（按句保存，中途停止再次启动自动续配）。

**⑤ 渲染环节**：配音完成后自动进渲染池 → 渲染面板显示每个场景合成进度 → 出片后网页直接在线播放预览。

**⑥ 全链路控制**：网页端总开关可随时【暂停】→ 全部worker停止取任务但不丢进度 → 【恢复】→ 从断点继续。

### 7.4 实时操作台的降级与安全

- 网页操作台挂掉不影响后台任务继续跑（任务队列独立进程）。
- 多设备访问需简单鉴权（本地无密码 / 局域网可加密码）。
- 一切操作留痕（操作日志），可回溯谁在何时做了什么。

---

## 第八部分 完整端到端流程（一次运行全貌）

```
一键启动 → 浏览器自动打开网页操作台
  ↓
网页端导入热点 / 粘贴反馈 / 确认频道定位
  ↓
任务队列初始化（自动恢复上次未完成任务）
  ↓
[智能体脚手架] 分析方向→批量选题→导演→写稿→审稿→改稿循环
  ↓（网页端实时刷新进度）
⛔ 人工确认闸：网页弹卡片确认（或全自动放行A级）
  ↓
[CosyVoice3] 按句配音（熊大/熊二音色），逐句落盘，可续传
  ↓
[三层模板] 新稿自动填充到视频模板对应位置
  ↓
[任务队列] 三池并行：写稿/配音/渲染同时推进多篇
  ↓
成片输出 data/output/videos/*.mp4（网页在线预览）
  ↓
[衔接制片台] 快速封面→成片审核→自动发布→数据回收→记忆沉淀
```

---

## 第九部分 落地清单（人力与资源）

### 9.1 需要准备的东西

| 项 | 说明 |
|---|---|
| LLM API Key | 写稿/审稿等文案Agent使用（OpenAI兼容接口），可用本地模型替代 |
| GPU环境 | 本地Windows+显存≥8G，跑CosyVoice3 |
| CosyVoice3模型权重 | 下载放入 models/ 目录 |
| 熊大熊二原声 | 各2-3段干净wav（10-30秒/段，无背景音乐） |
| 我的世界跑酷素材包 | 放入 data/assets/mc_parkour/ |
| 视频模板定义 | 用YAML定义一套（含片头/主画面/片尾及组件），也可网页端可视化编辑 |

### 9.2 四阶段落地

| 阶段 | 内容 | 验收标准 |
|---|---|---|
| 一 | 本地服务+网页操作台骨架跑通，智能体脚手架选题→写稿→审稿→确认闸 | 网页端输入定位+热点，产出合格稿件 |
| 二 | CosyVoice3接入+熊大熊二音色复刻+按句配音 | 一句话用熊大音色读出，能断点续传，网页看进度 |
| 三 | 三层模板引擎+我的世界跑酷背景+自动填充 | 换稿不重剪，网页一键出新片并在线预览 |
| 四 | 任务队列三池并行+全链路联调+网页实时操作 | 3篇以上同时推进，中断后恢复，全程网页操作 |

### 9.3 系统全自动程度说明

- **自动**：方向分析、选题、导演、写稿、改稿、审稿、配音、填充、渲染、合成、发布、数据回收。
- **人工**：仅【人工确认闸】一处确认步骤（可在设置中设为"全自动放行A级稿件"），且全部在网页端点击即可。

---

## 附录 数据目录约定

```
marketing-agent/
├── config/
│   ├── persona.yaml        # 频道定位（唯一人工必填）
│   └── llm.yaml            # 模型/密钥配置
├── prompts/                # 各Agent提示词模板
├── templates/              # 视频模板定义（YAML）
├── data/
│   ├── feedback/           # 历史反馈（评论/差评）
│   ├── hot/                # 热点信息
│   ├── voice_src/          # 熊大熊二原声
│   ├── assets/mc_parkour/  # 我的世界跑酷素材包
│   └── output/
│       ├── topics/         # 选题成果
│       ├── voice/          # 按句配音wav
│       └── videos/         # 最终成片
└── models/CosyVoice3-0.5B/ # TTS模型权重
```

## 附2 本地启动与网页端访问流程

```
首次：安装依赖 → 放置模型/素材/原声 → 配置API Key
日常：双击 启动.bat
  ↓
自动拉起：任务队列进程 + CosyVoice3服务 + Web服务
  ↓
浏览器自动打开 http://localhost:xxxx
  ↓
网页端完成：导入热点 → 触发选题 → 确认稿件 → 看配音进度 → 预览成片
关停：网页点【停止】或关闭窗口（进度已持久化，下次继续）
```
