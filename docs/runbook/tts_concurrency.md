# TTS 并发标定（T2.2 · 裁决 C8）

> 由 `scripts/bench_tts.py` 于 **2026-09-17 23:41** 真机跑出，**不要手改** —— 重跑覆盖。
> 显卡总显存 **8191 MB**；红线取 85%（留桌面占用）。
> 判定看**吞吐**（墙钟），不看单句快慢 —— 单句 RTF 只用来判断有没有被换页拖垮。
> 参考音色 **bigbear**。

| 并发 | 成功 | 失败 | 其中 OOM | 音频合计 | 墙钟 | 中位 RTF | 峰值显存 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 1 | 3 | 0 | 0 | 49.3s | 41.38s | 0.819 | 3643 MB |
| 2 | 3 | 0 | 0 | 47.2s | 36.67s | 1.344 | 4060 MB |
| 3 | 3 | 0 | 0 | 47.3s | 35.61s | 2.021 | 4090 MB |

**结论：`voice.concurrency = 1`** —— 最保守档并发 1 全绿、峰值 3643 MB ≤ 6962 MB；再往上加不划算 —— 并发 2 全绿、峰值 4060 MB 也没超线，但吞吐只快 11.4%（理想 100%，门槛 40%），却把中位 RTF 从 0.819 拖到 1.344 ⇒ 不值得，停在 1。

回写位置：`config/pools.yaml → pools.voice.concurrency`，以及 `config/tts.yaml → server.concurrency`（服务自己的信号量）。

> RTF = 合成耗时 / 音频时长。**它随并发上升**是正常的（GPU 要分时），
> 关键是别涨到比串行还慢 —— 那说明显存已经开始换页了。

> 升档门槛：吞吐增益要达到「理想增益」的 **40%** 才算数
> （并发翻倍 = 理想 +100%，即至少要快 40%）；拿不到就停，
> 别拿 RTF 去换那点吞吐 —— 这块卡还要分给 render 池。

> 本报告由 `docs\runbook\tts_concurrency.json` 的读数重出（**没有重跑 GPU**）。
