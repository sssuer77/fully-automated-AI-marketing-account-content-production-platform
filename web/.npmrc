# npm 缓存必须落 D 盘（R1：C 盘可用空间常年 < 2 GB，装一次前端依赖就能把它吃干）。
# 正常入口是 `tasks.ps1 web`（会 dot-source scripts/env.ps1 设 NPM_CONFIG_CACHE），
# 这一行是「忘了 source 也不会写爆 C 盘」的兜底。
cache=D:\ai_models\npm_cache
fund=false
audit=false
