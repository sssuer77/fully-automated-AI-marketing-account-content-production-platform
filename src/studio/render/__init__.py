"""渲染子系统（T3.x）—— 一期：单遍合成 + 固定水印（§04.2.8）。

依赖方向（§02.1）：`render/` **不 import FastAPI**；`agents/` 不得 import `render/`。
本包可以依赖 `core/` 与 `domain/`（纯数据 + 状态机，零外部依赖）。

一期范围（v3.2 · C13）
----------------------
跑酷循环裁长 → 缩放铺满 → 叠加**固定水印** → 混音 → **一次 ffmpeg 调用**直出 `final.mp4`，
无中间产物。**不做**句子↔镜头对齐（D2）、**不做**场景中间产物与三层模板编排（二期 P1）。

本包与 `core/outputs_store.py` 的分工
------------------------------------
`config/outputs.yaml` 是**真相**（T4.7 面板编辑的就是它），本包把它**翻译成 ffmpeg 的 argv**：

- `profiles.py`  —— profile → 编码/封装参数（唯一翻译点，`--show` 与 T3.3 共用）；
- `watermark.py` —— 水印参数模型 + PNG 实测 + **D5 硬门禁**（缺失 ⇒ 拒绝渲染，不降级）。
"""
