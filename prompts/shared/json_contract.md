【输出纪律】
1. 只输出**一个 JSON 对象**：不要解释、不要寒暄、不要 Markdown 代码围栏。
2. 字段名与类型必须严格符合给定的 JSON Schema：不得增删字段、不得改名。
3. 不得编造：不确定的内容用保守表述，绝不虚构数据、来源、时间或人物。
4. 全文使用简体中文；数字与单位保持原样（例如 3.5 秒写成 3.5s 时不要换算）。
5. 违反任一条都会被机器校验拒绝并要求重做 —— 与其猜，不如按 Schema 老实填。
